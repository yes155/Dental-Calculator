import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=r=>readFile(new URL(`../src/${r}/index.html`,import.meta.url),'utf8');
for(const[r,h]of[['dental-bridge-cost','Dental bridge cost'],['dentures-cost','Dentures cost']])test(`${r} safeguards`,async()=>{const x=await read(r);assert.match(x,/<meta name="robots" content="noindex,nofollow">/);assert.ok(x.includes(`<h1>${h}</h1>`));assert.equal((x.match(/<h1\b/g)||[]).length,1);assert.doesNotMatch(x,/\[SOURCE NEEDED BEFORE PUBLICATION\]|TODO|PLACEHOLDER|EDITOR NOTE|Reviewed by Juliana Maia Teixeira/);assert.match(x,/data-prosthetic-calculator/);});
test('DEN-024 keeps bridge type prices separate',async()=>{const x=await read('dental-bridge-cost');for(const t of['$5,197','$4,100–$9,650','$1,041','$800–$2,030','$1,569','$1,200–$2,910','$5,195','$4,000–$9,450'])assert.ok(x.includes(t));assert.doesNotMatch(x,/\$800–\$9,650 range for (a )?dental bridge/i);assert.doesNotMatch(x,/value="(5197|1041|1569|5195)"/);});
test('DEN-030 preserves per-implant unit and does not create all-denture range',async()=>{const x=await read('dentures-cost');assert.match(x,/Full implant-supported, per implant/);assert.match(x,/\$4,093/);assert.match(x,/\$2,830–\$7,203/);assert.match(x,/\$340–\$11,018 endpoints span/);assert.doesNotMatch(x,/dentures cost \$340–\$11,018/i);assert.doesNotMatch(x,/value="(4093|2022|2293|1784)"/);});
