{
  "name": "dental-calculator-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs && node scripts/verify-wave-a.mjs && node scripts/verify-orthodontics.mjs && node scripts/verify-cosmetic.mjs",
    "qa": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
