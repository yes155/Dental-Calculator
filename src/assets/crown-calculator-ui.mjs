import { evaluateCrownQuote, formatCrownUsd, parseCrownUsd } from "./crown-calculator-core.mjs";

const form=document.querySelector("#calculator");
if(!form) throw new Error("CALC-022 form not found.");
const steps=[...document.querySelectorAll(".calc-step[data-step]")];
const progress=[...document.querySelectorAll("[data-step-indicator]")];
const errorSummary=document.querySelector("#error-summary");
const oneTotalFields=document.querySelector("#one-total-fields");
const itemizedFields=document.querySelector("#itemized-fields");
const crownInputs=document.querySelector("#crown-inputs");
const insuranceFields=document.querySelector("#insurance-estimate-fields");
const result=document.querySelector("#result");
const resultTitle=document.querySelector("#result-title");
const resultValues=document.querySelector("#result-values");
const resultScope=document.querySelector("#result-scope");
const stateLabels={included:"Included",separately_quoted:"Separate charge",not_on_quote:"Not listed",unknown:"Not sure"};
const selected=name=>form.querySelector(`input[name="${CSS.escape(name)}"]:checked`)?.value??"";
const escapeHtml=value=>String(value).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);

function clearErrors(){errorSummary.hidden=true;errorSummary.replaceChildren();form.querySelectorAll('[aria-invalid="true"]').forEach(el=>el.removeAttribute("aria-invalid"));}
function fieldFor(error){return form.querySelector(`[data-field="${CSS.escape(error.field)}"]`)||form.querySelector(`[data-error-field="${CSS.escape(error.field)}"]`);}
function message(error){const map={missing_quote_mode:"Choose how the crown quote is written.",invalid_crown_count:"Enter the number of crowns shown on the written quote.",crown_scope_not_confirmed:"Confirm that the crown count comes from the written quote.",crown_count_mismatch:"Enter one amount for each crown in the itemized quote.",invalid_amount:"Enter a valid dollar amount.",missing_insurance_mode:"Choose what you know about insurance for this quote.",scope_not_confirmed:"Confirm that the insurance estimate covers the same written quote.",insurance_over_total:"The insurance estimate cannot be more than the quote total."};return map[error.code]??error.message;}
function renderErrors(errors){const h=document.createElement("h2");h.textContent="Check these details";const ul=document.createElement("ul");for(const error of errors){fieldFor(error)?.setAttribute("aria-invalid","true");const li=document.createElement("li");li.textContent=message(error);ul.append(li);}errorSummary.append(h,ul);errorSummary.hidden=false;errorSummary.focus();}
function setStep(n,focus=true){steps.forEach(step=>{step.hidden=Number(step.dataset.step)!==n;});progress.forEach(item=>{const x=Number(item.dataset.stepIndicator);item.classList.toggle("is-current",x===n);item.classList.toggle("is-complete",x<n);if(x===n)item.setAttribute("aria-current","step");else item.removeAttribute("aria-current");});clearErrors();if(focus)document.querySelector(`#calc-step-${n}-heading`)?.focus({preventScroll:true});}
function renderCrownInputs(){crownInputs.replaceChildren();const count=Number(document.querySelector("#crown-count").value);if(selected("quoteMode")!=="itemized_crowns"||!Number.isInteger(count)||count<1||count>32)return;for(let i=0;i<count;i+=1){const wrap=document.createElement("div");wrap.className="field-money-compact";wrap.innerHTML=`<label for="crown-${i}">Crown ${i+1} amount</label><div class="money"><span aria-hidden="true">$</span><input id="crown-${i}" data-crown-index="${i}" data-field="crownAmounts.${i}" inputmode="decimal" autocomplete="off" placeholder="0.00"></div>`;crownInputs.append(wrap);}}
function updateQuoteMode(){const mode=selected("quoteMode");oneTotalFields.hidden=mode!=="one_total";itemizedFields.hidden=mode!=="itemized_crowns";document.querySelector("#written-total").disabled=mode!=="one_total";renderCrownInputs();}
function buildComponent(id,label){const host=document.querySelector(`#${id}-choice`);host.dataset.errorField=`components.${id}.state`;host.innerHTML=`<legend>${label}</legend><div class="segmented-options">${Object.entries(stateLabels).map(([value,text])=>`<label class="segment-option"><input type="radio" name="${id}Status" value="${value}"${value==="unknown"?" checked":""}><span>${text}</span></label>`).join("")}</div><div class="component-amount field-money-compact" hidden><label for="${id}-amount">Separate charge amount</label><div class="money"><span aria-hidden="true">$</span><input id="${id}-amount" data-field="components.${id}.amount" inputmode="decimal" autocomplete="off" placeholder="0.00" disabled></div></div>`;host.addEventListener("change",event=>{if(event.target.name!==`${id}Status`)return;const show=event.target.value==="separately_quoted";const wrap=host.querySelector(".component-amount");const input=wrap.querySelector("input");wrap.hidden=!show;input.disabled=!show;if(!show)input.value="";});}
function updateInsurance(){const show=selected("insuranceMode")==="entered_estimate";insuranceFields.hidden=!show;document.querySelector("#insurance-amount").disabled=!show;document.querySelector("#same-scope").disabled=!show;}
function validateStepOne(){const errors=[];const mode=selected("quoteMode");if(!mode)errors.push({field:"quoteMode",code:"missing_quote_mode",message:""});const count=Number(document.querySelector("#crown-count").value);if(!Number.isInteger(count)||count<1||count>32)errors.push({field:"crownCount",code:"invalid_crown_count",message:""});if(!document.querySelector("#crown-count-confirmed").checked)errors.push({field:"crownCountConfirmed",code:"crown_scope_not_confirmed",message:""});if(mode==="one_total"&&!parseCrownUsd(document.querySelector("#written-total").value).ok)errors.push({field:"writtenTotal",code:"invalid_amount",message:""});if(mode==="itemized_crowns"&&Number.isInteger(count)&&count>=1&&count<=32){const inputs=[...crownInputs.querySelectorAll("input")];if(inputs.length!==count)errors.push({field:"crownAmounts",code:"crown_count_mismatch",message:""});inputs.forEach((input,i)=>{if(!parseCrownUsd(input.value).ok)errors.push({field:`crownAmounts.${i}`,code:"invalid_amount",message:""});});}return errors;}
function collect(){const component=id=>({state:selected(`${id}Status`),amount:document.querySelector(`#${id}-amount`).value});return{quoteMode:selected("quoteMode"),crownCount:document.querySelector("#crown-count").value,crownCountConfirmed:document.querySelector("#crown-count-confirmed").checked,writtenTotal:document.querySelector("#written-total").value,crownAmounts:[...crownInputs.querySelectorAll("input")].map(input=>input.value),crownMaterial:document.querySelector("#crown-material").value,components:{buildup:component("buildup"),examImaging:component("examImaging"),other:component("other")},insurance:{mode:selected("insuranceMode"),amount:document.querySelector("#insurance-amount").value,sameScopeConfirmed:document.querySelector("#same-scope").checked}};}
function renderResult(out){result.hidden=false;result.classList.toggle("result-incomplete",out.status==="incomplete");resultTitle.textContent=out.status==="incomplete"?"Some parts of your quote are still unclear":"Your crown quote summary";const avg=`${out.averageApproximate?"About ":""}${formatCrownUsd(out.averagePerCrownCents)}`;const patient=out.patientCents===null?"Not shown":formatCrownUsd(out.patientCents);resultValues.innerHTML=`<dl class="result-grid result-grid--guided"><div class="result-primary"><dt>Total from your quote</dt><dd>${formatCrownUsd(out.totalCents)}</dd></div><div><dt>Average per entered crown</dt><dd>${avg}</dd></div><div><dt>Amount after insurance estimate</dt><dd>${patient}</dd></div></dl>`;const material=document.querySelector("#crown-material").selectedOptions[0].textContent;resultScope.innerHTML=`<div class="result-chips"><span>${out.crownCount} ${out.crownCount===1?"crown":"crowns"} confirmed</span><span>${escapeHtml(material)}</span></div><h3>Other quote items</h3><ul class="scope-summary">${out.componentRows.map(row=>`<li><span>${escapeHtml(row.label)}</span><strong>${escapeHtml(stateLabels[row.state])}${row.amountCents!==null?` · ${formatCrownUsd(row.amountCents)}`:""}</strong></li>`).join("")}</ul>`;if(out.status==="incomplete")resultScope.insertAdjacentHTML("beforeend",'<p class="notice-inline"><strong>Scope incomplete:</strong> at least one item is marked “Not sure.” The total includes only amounts you entered.</p>');result.scrollIntoView({behavior:window.matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth",block:"start"});}

buildComponent("buildup","Build-up or foundation");buildComponent("examImaging","Exam or imaging");buildComponent("other","Other quoted charge");updateQuoteMode();updateInsurance();setStep(1,false);
form.addEventListener("change",event=>{if(event.target.name==="quoteMode")updateQuoteMode();if(event.target.name==="insuranceMode")updateInsurance();});
document.querySelector("#crown-count").addEventListener("input",renderCrownInputs);
document.querySelector("#step-1-next").addEventListener("click",()=>{clearErrors();const errors=validateStepOne();if(errors.length)return renderErrors(errors);setStep(2);});
document.querySelector("#step-2-back").addEventListener("click",()=>setStep(1));document.querySelector("#step-2-next").addEventListener("click",()=>setStep(3));document.querySelector("#step-3-back").addEventListener("click",()=>setStep(2));
form.addEventListener("submit",event=>{event.preventDefault();clearErrors();const out=evaluateCrownQuote(collect());if(out.status==="invalid"){result.hidden=true;renderErrors(out.errors);return;}renderResult(out);});
form.addEventListener("reset",()=>requestAnimationFrame(()=>{clearErrors();form.querySelectorAll("input[type=radio]").forEach(input=>{input.checked=false;});["buildup","examImaging","other"].forEach(id=>{form.querySelector(`input[name="${id}Status"][value="unknown"]`).checked=true;});document.querySelector("#crown-count").value="1";document.querySelector("#crown-count-confirmed").checked=false;document.querySelector("#written-total").value="";document.querySelector("#crown-material").value="not_stated";document.querySelector("#same-scope").checked=false;document.querySelector("#insurance-amount").value="";crownInputs.replaceChildren();form.querySelectorAll(".component-amount").forEach(wrap=>{wrap.hidden=true;});form.querySelectorAll(".component-amount input").forEach(input=>{input.disabled=true;input.value="";});updateQuoteMode();updateInsurance();result.hidden=true;setStep(1,false);}));
